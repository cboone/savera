{
  "name": "savera-text-lint-tools",
  "version": "0.0.0",
  "private": true,
  "description": "Pins the tools that format and verify this repository's text. Not a package, not published, and not part of any build: zig build never reads this file.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "markdownlint-cli2": "0.23.2",
    "markdownlint-rule-relative-links": "5.1.2",
    "prettier": "3.9.8"
  },
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint:md": "markdownlint-cli2 \"**/*.md\""
  }
}
